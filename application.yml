spring:
  datasource:
    url: jdbc:postgresql://localhost:5431/loggains
    username: kamil
    password: kamil_compose
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: loggainsbusiness@gmail.com
    password: LogGainsBusiness420
    properties:
      mail:
        smtp:
          auth: true
          starttls.enable: true

logging:
  level:
    org:
      springframework:
        security: DEBUG
        web: DEBUG


