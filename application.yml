spring:
  datasource:
    url: jdbc:postgresql://localhost:5431/loggains
    username: kamil
    password: kamil_compose
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false

logging:
  level:
    org:
      springframework:
        security: DEBUG
